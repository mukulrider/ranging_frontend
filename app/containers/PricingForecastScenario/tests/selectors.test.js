// import { fromJS } from 'immutable';
// import { makeSelectPricingForecastScenarioDomain } from '../selectors';

// const selector = makeSelectPricingForecastScenarioDomain();

describe('makeSelectPricingForecastScenarioDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
