// import { fromJS } from 'immutable';
// import { makeSelectRangingScenarioTrackerPageDomain } from '../selectors';

// const selector = makeSelectRangingScenarioTrackerPageDomain();

describe('makeSelectRangingScenarioTrackerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
