// import { fromJS } from 'immutable';
// import { makeSelectRangingViewDelistScenarioPageDomain } from '../selectors';

// const selector = makeSelectRangingViewDelistScenarioPageDomain();

describe('makeSelectRangingViewDelistScenarioPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
