// import { fromJS } from 'immutable';
// import { makeSelectRangingViewScenarioPageDomain } from '../selectors';

// const selector = makeSelectRangingViewScenarioPageDomain();

describe('makeSelectRangingViewScenarioPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
